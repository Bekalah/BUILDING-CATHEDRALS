name: Auto Fix (Local Stub)
on: { workflow_dispatch: {} }
jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-fix CI disabled locally. Restore full workflow from .github/workflows-archive/auto-fix.orig.yml"
