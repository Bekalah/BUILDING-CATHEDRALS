name: Azure SWA Deploy (Local Stub)
on: { workflow_dispatch: {} }
jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Azure SWA deployment disabled locally. Restore from archive for real deployments."
